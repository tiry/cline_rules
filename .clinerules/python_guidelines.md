##  Python Project Standards

For Python-based projects:

* Use a `venv` for virtual environments
* Define dependencies in a `pyproject.toml`
* Use `pytest` for unit testing

The code should be properly documented and should contain type hinting.


